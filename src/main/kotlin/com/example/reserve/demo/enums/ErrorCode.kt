package com.example.reserve.demo.enums

enum class ErrorCode(val value: String) {
    RESULT_NULL("result null"), AVAILABLE_RESERVE_OVER_TIME("unavailable reserve this office seat"), INTERNAL_SERVER_ERROR("internal server error")

}